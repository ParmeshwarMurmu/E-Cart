

const initialState = {
    userOrder: [],
    isLoading: false,
    isError: false,
    isData: false
}

export const reducer = (state = initialState, {type, payload})=>{

    switch (type) {
        
    
        default:
            break;
    }
}